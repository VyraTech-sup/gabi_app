{
  "buildCommand": "cd mobile && npm install && npx expo export --platform web --output-dir dist",
  "outputDirectory": "mobile/dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "\/(.*)", "dest": "\/index.html" }
  ]
}
